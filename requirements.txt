# Gatehouse P1-P3 dependency manifest (pinned loosely until lockfile lands in P1)
# Install: uv pip install -r requirements.txt

# Agent core
strands-agents>=0.2
strands-agents-tools>=0.2

# AWS
boto3>=1.34

# Schemas and config
pydantic>=2.7
pydantic-settings>=2.2
PyYAML>=6.0

# Gateway API (P3)
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27

# Quality gates
pytest>=8.2
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
